@model FaultReportingSystem.Models.HelpDesk
@{
    ViewData["Title"] = "Help Desk Staff Login";
    var LoginErrorMessage = TempData["LoginErrorMessage"] as string;
}

<div class="container">
    <div class="row justify-content-center">
        <h1 class="text-center">Help Desk Staff Login</h1>
        <div class="col-md-4">
            <hr />
            @if (!string.IsNullOrEmpty(LoginErrorMessage))
            {
                <div class="alert alert-danger alert-fade" role="alert">
                    @LoginErrorMessage
                </div>
            }
            @using (Html.BeginForm("LoginHelpDeskStaff", "Home", FormMethod.Post, new { @class = "needs-validation", novalidate = "true" }))
            {
                <div class="form-group">
                    <label asp-for="HelpDeskEmail" class="control-label">Email</label>
                    <input asp-for="HelpDeskEmail" type="email" class="form-control" maxlength="250" required="required" />
                    <span asp-validation-for="HelpDeskEmail" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="HelpDeskPassword" class="control-label">Password</label>
                    <input asp-for="HelpDeskPassword" type="password" class="form-control" maxlength="25" required="required" />
                    <span asp-validation-for="HelpDeskPassword" class="text-danger"></span>
                </div>
                <br />
                <div class="form-group d-grid">
                    <button type="submit" class="btn btn-outline-primary">Login</button>
                </div>
                <br />
            }
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}